language: java
jdk:
  - openjdk8

branches:
  only:
  - master

# Travis CI 서버의 Home
cache:
  directories:
  - '$HOME/.m2/repository'
  - '$HOME/.mvn'

script: "./mvnw clean package"

# CI 성공시 이메일로 알림
notifications:
  email:
    recipients:
      - eojin312@naver.com